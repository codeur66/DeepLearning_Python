pip install tensorflow
pip install keras
pip install matplotlib
sudo apt install graphviz # for keras.utils.plot_model, (optional installation, for debugging)
pip install pydot  # for keras.utils.plot_model
pip install psutil # to see the open files and close them (optional installation, for many different files)
