pip install tensorflow
pip install keras
pip install matplotlib
sudo apt install graphviz # for keras.utils.plot_model, (optional installation, for debugging)
pip install pydot  # for keras.utils.plot_model
pip install psutil # to see the open files and close them (optional installation, for many different files)


# Use on CI Jenkins server on Github repo:

sudo su
systemctl start jenkins
systemctl status jenkins

stores files here: /var/jenkins_home.

# access jeknins:
http://myServer:8080
http://localhost:8080

# logs:
/var/log/jenkins/jenkins.log

# find publicIP for github webhook
https://whatismyipaddress.com/ip/

# install or check if is installed the git and github plugin in jenkins

# install ServerXP client to make your localIP public accessible to Jenkins.
curl -O https://portal.socketxp.com/download/linux/socketxp && chmod +wx socketxp && sudo mv socketxp /usr/local/bin

# create an account to connect your SocketXP Agent with the SocketXP Cloud Service.
# Get an auth tokken and you copy paste it to your terminal.
To create a tunnel, allowing you to expose local or private web servers to the public.
Get the publicURL from the tunnel and paste it into the Github Webhook.


snap install relay

https://webhookrelay.com/blog/2017/11/23/github-jenkins-guide/